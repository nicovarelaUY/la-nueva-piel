{
  "name": "lanuevapiel",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scanTailwind": "npx tailwindcss -i ./src/css/config.css -o ./src/css/estilos.css --watch",
    "build": "tailwindcss build src/css/estilos.css -o dist/styles.css",
    "copy-assets": "copyfiles -u 1 src/fonts/**/* src/img/**/* dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.7",
    "copyfiles": "^2.4.1",
    "tailwindcss": "^3.3.3"
  },
  "dependencies": {
    "@preline/collapse": "^2.0.1",
    "preline": "^2.0.2"
  }
}
